s3_bucket_names = ["paas-app-package-directory", "paas-buildpack-directory", "paas-droplet-directory", "paas-resource-directory"]

s3_users = ["bosh-s3"]

s3_replication_region = "someRegion"

blobstore_access_key_id="some aws access id"
blobstore_secret_access_key="some aws access key"

resource_directory_key="some directory name"
app_package_directory_key="some other directory name"
droplet_directory_key="some other other directory name"
buildpack_directory_key="last other directory name"

