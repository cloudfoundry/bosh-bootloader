#!/bin/bash -eu

ROOT_DIRECTORY="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

pushd "${ROOT_DIRECTORY}" > /dev/null
  go test -v ./bbl ./application ./commands ./commands/aws
popd > /dev/null
